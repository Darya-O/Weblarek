# Проектная работа "Веб-ларек"

Стек: HTML, SCSS, TS, Vite

Структура проекта:
- src/ — исходные файлы проекта
- src/components/ — папка с JS компонентами
- src/components/base/ — папка с базовым кодом

Важные файлы:
- index.html — HTML-файл главной страницы
- src/types/index.ts — файл с типами
- src/main.ts — точка входа приложения
- src/scss/styles.scss — корневой файл стилей
- src/utils/constants.ts — файл с константами
- src/utils/utils.ts — файл с утилитами

## Установка и запуск
Для установки и запуска проекта необходимо выполнить команды

```
npm install
npm run dev
```

или

```
yarn
yarn dev
```
## Сборка

```
npm run build
```

или

```
yarn build
```
# Интернет-магазин «Web-Larёk»
«Web-Larёk» — это интернет-магазин с товарами для веб-разработчиков, где пользователи могут просматривать товары, добавлять их в корзину и оформлять заказы. Сайт предоставляет удобный интерфейс с модальными окнами для просмотра деталей товаров, управления корзиной и выбора способа оплаты, обеспечивая полный цикл покупки с отправкой заказов на сервер.

## Архитектура приложения

Код приложения разделен на слои согласно парадигме MVP (Model-View-Presenter), которая обеспечивает четкое разделение ответственности между классами слоев Model и View. Каждый слой несет свой смысл и ответственность:

Model - слой данных, отвечает за хранение и изменение данных.  
View - слой представления, отвечает за отображение данных на странице.  
Presenter - презентер содержит основную логику приложения и  отвечает за связь представления и данных.

Взаимодействие между классами обеспечивается использованием событийно-ориентированного подхода. Модели и Представления генерируют события при изменении данных или взаимодействии пользователя с приложением, а Презентер обрабатывает эти события используя методы как Моделей, так и Представлений.

### Базовый код

#### Класс Component
Является базовым классом для всех компонентов интерфейса.
Класс является дженериком и принимает в переменной `T` тип данных, которые могут быть переданы в метод `render` для отображения.

Конструктор:  
`constructor(container: HTMLElement)` - принимает ссылку на DOM элемент за отображение, которого он отвечает.

Поля класса:  
`container: HTMLElement` - поле для хранения корневого DOM элемента компонента.

Методы класса:  
`render(data?: Partial<T>): HTMLElement` - Главный метод класса. Он принимает данные, которые необходимо отобразить в интерфейсе, записывает эти данные в поля класса и возвращает ссылку на DOM-элемент. Предполагается, что в классах, которые будут наследоваться от `Component` будут реализованы сеттеры для полей с данными, которые будут вызываться в момент вызова `render` и записывать данные в необходимые DOM элементы.  
`setImage(element: HTMLImageElement, src: string, alt?: string): void` - утилитарный метод для модификации DOM-элементов `<img>`


#### Класс Api
Содержит в себе базовую логику отправки запросов.

Конструктор:  
`constructor(baseUrl: string, options: RequestInit = {})` - В конструктор передается базовый адрес сервера и опциональный объект с заголовками запросов.

Поля класса:  
`baseUrl: string` - базовый адрес сервера  
`options: RequestInit` - объект с заголовками, которые будут использованы для запросов.

Методы:  
`get(uri: string): Promise<object>` - выполняет GET запрос на переданный в параметрах ендпоинт и возвращает промис с объектом, которым ответил сервер  
`post(uri: string, data: object, method: ApiPostMethods = 'POST'): Promise<object>` - принимает объект с данными, которые будут переданы в JSON в теле запроса, и отправляет эти данные на ендпоинт переданный как параметр при вызове метода. По умолчанию выполняется `POST` запрос, но метод запроса может быть переопределен заданием третьего параметра при вызове.  
`handleResponse(response: Response): Promise<object>` - защищенный метод проверяющий ответ сервера на корректность и возвращающий объект с данными полученный от сервера или отклоненный промис, в случае некорректных данных.

#### Класс EventEmitter
Брокер событий реализует паттерн "Наблюдатель", позволяющий отправлять события и подписываться на события, происходящие в системе. Класс используется для связи слоя данных и представления.

Конструктор класса не принимает параметров.

Поля класса:  
`_events: Map<string | RegExp, Set<Function>>)` -  хранит коллекцию подписок на события. Ключи коллекции - названия событий или регулярное выражение, значения - коллекция функций обработчиков, которые будут вызваны при срабатывании события.

Методы класса:  
`on<T extends object>(event: EventName, callback: (data: T) => void): void` - подписка на событие, принимает название события и функцию обработчик.  
`emit<T extends object>(event: string, data?: T): void` - инициализация события. При вызове события в метод передается название события и объект с данными, который будет использован как аргумент для вызова обработчика.  
`trigger<T extends object>(event: string, context?: Partial<T>): (data: T) => void` - возвращает функцию, при вызове которой инициализируется требуемое в параметрах событие с передачей в него данных из второго параметра.

#### Модели данных
В приложении существуют три класса-модели, каждый из которых отвечает за собственную область данных и инкапсулирует всю логику работы с ней.

#### Класс ProductCatalog (каталог товаров)
Область ответственности: этот класс ведёт учёт всего ассортимента доступных товаров и управляет состоянием «выбранного в данный момент товара» для показа подробной информации. Он не касается содержимого корзины и не хранит персональные данные клиента.

Конструктор: constructor() – не требует аргументов; наполнение каталога осуществляется через публичные методы.

Поля класса:

private products: IProduct[] – список всех товаров, имеющихся в продаже (массив объектов, соответствующих интерфейсу IProduct).

private selectedProduct: IProduct | null – товар, который выбран для детального просмотра; если ничего не выбрано – null.

Методы:

setProducts(products: IProduct[]): void – загружает переданный массив как актуальный каталог.

getProducts(): IProduct[] – возвращает полный список товаров из каталога.

getProductById(id: string): IProduct | undefined – выполняет поиск товара по его уникальному id; при отсутствии возвращает undefined.

setSelectedProduct(product: IProduct): void – фиксирует товар для показа в режиме детального просмотра.

getSelectedProduct(): IProduct | null – возвращает текущий выбранный товар либо null, если таковой не установлен.

#### Класс Cart (корзина)
Область ответственности: класс управляет наполнением корзины покупателя. Он содержит перечень товаров, отобранных для покупки, и предоставляет инструменты для изменения этого перечня, а также для подсчёта итоговых показателей (общая сумма, число позиций).

Конструктор: constructor() – без параметров; корзина изначально пуста.

Поля класса:

private items: IProduct[] – массив товаров, которые в данный момент лежат в корзине (элементы соответствуют интерфейсу IProduct и могут быть как копиями, так и ссылками на объекты из каталога).

Методы:

getItems(): IProduct[] – возвращает актуальный список всех позиций в корзине.

addItem(product: IProduct): void – добавляет указанный товар в корзину.

removeItem(productId: string): void – удаляет из корзины товар с заданным productId (если таких несколько – убирается первый попавшийся).

clear(): void – полностью очищает корзину.

getTotalPrice(): number – вычисляет суммарную стоимость всех товаров в корзине: для подсчёта берётся поле price; если у товара цена равна null, он считается бесплатным (цена 0).

getItemCount(): number – возвращает общее количество товаров (длину массива items).

hasItem(productId: string): boolean – проверяет, присутствует ли в корзине товар с переданным идентификатором; возвращает true при наличии, иначе false.

#### Класс Buyer (покупатель)
Область ответственности: хранит данные о покупателе, необходимые для оформления заказа, и занимается их проверкой. Класс устроен так, что позволяет изменять отдельные поля по отдельности, не затрагивая остальные, а также даёт детальную информацию об ошибках в заполнении.

Конструктор: constructor() – без параметров; все данные устанавливаются через методы.

Поля класса:

private payment: TPayment | null – выбранный способ оплаты (может быть null, если ещё не указан).

private email: string = '' – электронная почта.

private phone: string = '' – контактный телефон.

private address: string = '' – адрес доставки.

Методы:

setData(data: Partial<IBuyer>): void – частично обновляет информацию о покупателе: принимает объект с любым набором полей из IBuyer и заменяет только те, что переданы, остальные оставляя без изменений.

getData(): IBuyer – возвращает полный объект с текущими данными покупателя, соответствующий интерфейсу IBuyer.

clear(): void – сбрасывает все поля: строковые становятся пустыми, а payment становится undefined.

validate(): BuyerValidationErrors – проверяет корректность заполнения всех полей. Результат – объект, в котором ключи – это имена полей, а значения – сообщения об ошибках для тех полей, что не прошли проверку. Если поле заполнено верно, оно отсутствует в выдаче. Правило: поле считается невалидным, если его значение – пустая строка. Пример возврата при ошибках: { payment: 'Не выбран вид оплаты', email: 'Укажите email' }. Если все поля заполнены, возвращается пустой объект {}.

#### Слой коммуникации
#### Класс ApiService
Область ответственности: выступает посредником между приложением и сервером. Обеспечивает загрузку каталога товаров и отправку оформленного заказа.

Конструктор: принимает параметр api: IApi – экземпляр готового API-клиента из начального набора.

Методы:

getProducts(): Promise<IProductsResponse> – отправляет GET-запрос на эндпоинт /product/ и возвращает промис с объектом, содержащим массив товаров и их общее количество.

sendOrder(orderData: IOrderRequest): Promise<IOrderResponse> – отправляет POST-запрос на эндпоинт /order/ с данными заказа (список товаров и информация о покупателе). В ответ приходит объект подтверждения, содержащий идентификатор заказа, итоговую сумму и служебное сообщение.

Зависимости: использует типы, описанные в types/index.ts.

#### Пересказ документации слоя Представления (View) своими словами
Общая идея
Этот документ — руководство по визуальной части интернет-магазина. Здесь описано, из каких визуальных кирпичиков состоит интерфейс, какую информацию они показывают и как реагируют на действия пользователя. Каждый элемент интерфейса — это отдельный класс, который знает только свою работу.

Что за чем показать (интерфейсы данных)
Это список требований к информации, которая нужна каждому элементу на странице.

Элемент         	        Что ему нужно
Шапка сайта	                Только число — сколько товаров в корзине
Галерея товаров	            Список готовых DOM-элементов карточек
Модальное окно	            Один DOM-элемент, который будет показан внутри
Карточка товара (любая)	    Название и цена
Карточка в каталоге	        Название, цена, категория, картинка
Карточка в модалке	        Всё из каталога + описание, текст кнопки, активна ли кнопка
Карточка в корзине	        Название, цена, порядковый номер
Вся корзина	                Список карточек и общая сумма
Форма (общая)	            Валидна ли форма и текст ошибки
Форма оплаты и адреса	    Способ оплаты и адрес
Форма контактов	            Email и телефон
Экран успеха	            Итоговая сумма заказа
Важно: интерфейсы только описывают, какие данные нужны — они сами данные не хранят и не обрабатывают.

Кто за что отвечает (классы)
Все визуальные компоненты делятся на группы по своим задачам.

Шапка сайта (Header)
Её работа: показывать, сколько товаров в корзине, и при клике на иконку корзины сообщать, что пора открывать корзину.

Что умеет: обновлять цифру счётчика.

Галерея (Gallery)
Её работа: принимать готовые карточки товаров и выводить их на главной странице.

Что умеет: полностью заменить содержимое галереи новым набором карточек.

Модальное окно (Modal)
Его работа: быть универсальным контейнером — внутрь можно положить любой контент, открыть, закрыть, а при закрытии очистить содержимое.

Что умеет: открываться, закрываться, менять содержимое. Само следит за кликами по крестику и по затемнённому фону.

Карточки товаров (Card, CatalogCard, PreviewCard, BasketCard)
Это семейство классов, где есть общий предок и три специализированных наследника.

Класс	        -Где используется	    -Что добавляет к общему
Card (базовый)	 Напрямую	            Показывает название и цену
CatalogCard	     На главной странице	Категорию, картинку, клик по карточке
PreviewCard	     В модальном окне	    Описание, кнопку, управление активностью кнопки
BasketCard	     В корзине	            Порядковый номер, кнопку удаления
Важно: базовый класс не знает про клики — обработчики действий передаются наследникам извне.

Корзина как целое (BasketView)
Её работа: показывать список товаров, общую сумму и кнопку для перехода к оформлению.

Что умеет: обновлять список, обновлять итоговую сумму, реагировать на клик по кнопке оформления.

Формы (Form, OrderForm, ContactsForm)
Тоже семейство: общий предок и две специализированные формы.

Класс	Что делает
Form (базовый)	Следит за изменениями полей, отправкой, управляет кнопкой, показывает ошибки
OrderForm	Специально для выбора оплаты и ввода адреса
ContactsForm	Специально для ввода email и телефона
Важно: формы сами не знают, валидны ли данные — они только показывают состояние валидности, которое им передают.

Экран успеха (Success)
Его работа: показать сообщение, что заказ оформлен, итоговую сумму и кнопку для возврата к покупкам.

Как части общаются друг с другом (события)
Вся коммуникация построена на событиях — как радиосигналах. Компонент что-то делает → отправляет сигнал → кто-то другой ловит сигнал и реагирует.

Событие	        Кто генерирует	        Что означает
basket:open 	Шапка	                Пользователь кликнул по корзине
modal:close	    Модальное окно	        Пользователь закрыл модалку
order:open	    Корзина	                Пользователь нажал «Оформить»
order.payment:change	Форма оплаты	Пользователь выбрал способ оплаты
order.address:change	Форма оплаты	Пользователь ввёл адрес
order:submit	        Форма оплаты	Пользователь отправил первую форму
contacts.email:change	Форма контактов	Пользователь ввёл email
contacts.phone:change	Форма контактов	Пользователь ввёл телефон
contacts:submit	Форма контактов	Пользователь отправил контакты
Важно: большинство событий не передают данные — только факт действия. Кто слушает событие — сам идёт и забирает данные из моделей.

События от моделей
Модели данных тоже отправляют сигналы, когда что-то меняется:

Событие	Что изменилось
catalog:changed	    Обновился список товаров
preview:changed	    Выбран другой товар для просмотра
basket:changed	    Изменилась корзина
buyer:changed	    Изменились данные покупателя
Эти сигналы говорят: «Эй, данные обновились, обнови интерфейс!»

#### Главный дирижёр (Презентер)
Презентер — это координатор, который связывает всё воедино. Он:

Создаёт все модели данных и все визуальные компоненты

Слушает все события, которые происходят в приложении

Знает, что делать в ответ на каждое событие

Берёт свежие данные из моделей и передаёт их в интерфейс

Управляет порядком: каталог → просмотр товара → корзина → формы → отправка заказа

Важно: Презентер не хранит данные — он только передаёт их туда-сюда. И не трогает DOM — это работа классов представления.

Как всё начинается
При загрузке страницы:
Корзина очищается,
Данные покупателя сбрасываются,
Модели отправляют сигнал «я изменился»,
Презентер ловит сигнал и обновляет интерфейс,
Пользователь видит пустую корзину и чистые формы.
Главные принципы

Разделение обязанностей	- Каждый класс занимается только своим делом
Единый источник правды - Все данные только в моделях, интерфейс их только показывает.
Обновление по сигналу	Данные изменились → модель сигналит → презентер обновляет интерфейс
Один канал связи	Все события идут через один брокер EventEmitter
Презентер не хранит	Данные только в моделях
Коротко: о чём весь документ?
Это архитектура визуальной части интернет-магазина, где:

Каждый кусочек экрана — отдельный класс со своей зоной ответственности

Компоненты общаются через события, а не напрямую

Данные отделены от интерфейса и хранятся в моделях

Есть координатор (Презентер), который связывает модели и интерфейс

Всё построено так, чтобы части можно было менять независимо.

